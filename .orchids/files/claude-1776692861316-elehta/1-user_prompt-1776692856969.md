Refine the existing Meridiem Client Portal into a stable, production-ready system. Do NOT rebuild the entire app. Make focused improvements only.

---

## 1. REMOVE DEMO / PREVIEW MODE

* Remove all auto-login, auto-loaded demo dashboards, and preview-only behavior
* Remove any demo banners or temporary preview logic
* Remove fake/demo data dependencies
* Ensure the app does NOT reset, reload, or close unexpectedly
* Stabilize navigation so users can move between pages without interruption

---

## 2. AUTHENTICATION + REAL USER SETUP

Convert the system to support real usage:

* Keep login page clean and functional
* Support manual account creation
* Allow creation of an ADMIN account (full system access)
* Support CLIENT accounts tied to a specific company

Add admin-only capabilities:

* Create a new client (company name + basic info)
* Add users to a client
* Assign roles:

  * client_owner
  * client_billing
  * client_viewer

Ensure:

* Admin can see all clients
* Clients can ONLY see their own data
* Remove any demo credential dependencies

---

## 3. BRANDING + DESIGN UPDATE

Update the UI to match Meridiem branding:

* Replace white backgrounds with a soft cream color
* Maintain strong contrast for readability
* Keep navy as the primary color
* Use subtle gradient accents (sunrise/sunset tones)
* Ensure all cards and sections sit cleanly on the cream background

Header:

* Replace any placeholder “M” icon with the uploaded Meridiem logo
* Ensure logo scales properly for mobile and desktop

---

## 4. MOBILE + UX STABILITY

* Ensure the app is mobile-first (optimized for iPhone)
* Add clean spacing and padding throughout
* Use card-based layouts
* Ensure buttons are tap-friendly
* Fix any layout shifting or unstable behavior

Navigation:

* Ensure consistent navigation between:

  * Dashboard
  * My Team
  * Hours & Activity
  * Screenshots
  * Billing
  * Invoice History
  * Requests
  * Chat
  * Settings

---

## 5. ADMIN VISIBILITY + CONTROL

Ensure admin experience is fully usable:

* Admin can view all clients
* Admin can click into a client account
* Implement “View as Client” mode
* Show clear indicator when in this mode
* Allow easy exit back to admin view

Add:

* Internal notes section per client (admin only)
* Basic audit visibility for changes

---

## 6. DATA HANDLING (NO INTEGRATIONS YET)

* Keep existing structure for hours, billing, and screenshots
* Remove reliance on demo data, but allow empty states
* Add clean empty states:

  * “No workers yet”
  * “No invoices yet”
  * “No activity yet”

Ensure UI does NOT break when data is missing

---

## 7. OVERALL GOAL

The system should now feel like a real product, not a demo.

* Stable
* Clean
* Brand-aligned
* Ready for real clients to be added manually

Do not introduce integrations yet.
Do not rebuild existing pages.
Focus on stability, usability, and production readiness.
